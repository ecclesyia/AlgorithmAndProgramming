# Kotlin Basics: Input and Output

Kotlin is a concise OOP language running on the JVM. It uses `println()` and `readLine()`.

### How to compile and run:
```bash
kotlinc basics.kt -include-runtime -d basics.jar
java -jar basics.jar
```
